apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ktharatt.fullname" . }}-test-connection"
  labels:
    {{- include "ktharatt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ktharatt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
